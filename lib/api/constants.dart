const baseUrl = '';
const batchWarningsUrl = baseUrl + '';
const productsUrl = baseUrl + '';
const productsSyncUrl = baseUrl + '';
const productBatchesUrl = baseUrl + '';
const syncBatchesUrl = baseUrl + '';

const uploadBatchHeaders = {
  'Authorization': '',
  'Content-Type': 'application/json',
};
